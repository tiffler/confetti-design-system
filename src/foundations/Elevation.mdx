import { Meta } from '@storybook/addon-docs/blocks';
import { ElevationStack, ShadowSpecimen, CardElevation } from './Specimens';

<Meta title="Foundations/Elevation" />

# Elevation

Confetti has no blur-based elevation. Depth comes from two things: a small surface
ramp, and a hard offset shadow on hover.

## Surface ramp

Four steps. These are distinct surfaces, not opacity variants of one another —
the exact values came out of the homepage hero widget code.

<ElevationStack
  steps={[
    { token: 'color-surface-backdrop', label: 'recessed mat below page — app frames, control tracks' },
    { token: 'color-surface-page', label: 'page background baseline' },
    { token: 'color-surface-raised', label: 'widget / canvas surface, one step up from page' },
    { token: 'color-surface-panel', label: 'toolbar, sidebar, floating panel chrome' },
    { token: 'color-surface-card', label: 'card interiors, contrast surfaces' },
  ]}
/>

## Lift

`--shadow-lift` is a 3px hard offset with no blur, paired with a
`translate(-2px, -2px)`. Buttons and interactive cards share it, which is what makes
the whole system read as one sticker language.

In dark mode the shadow inverts to cream — a dark shadow on a dark surface reads as
nothing, so the token repoints rather than the component changing.

<ShadowSpecimen />

## Card elevation

The Card component is where the two mechanisms meet: it sits on a surface from the ramp
above, and interactive cards add the lift on hover. Both states below are built from the
live `--card-*` tokens, so they cannot drift from the component.

<CardElevation />

Interactive cards also turn slightly as they lift — square at rest, angled on hover.
See **Components → Card → Tilted grid**. The shadow reads as an extrusion of the card's own
2px border, which is why every variant carries that border even when its fill already
separates it from the page.

Both the transform and the transition are tokens
(`--button-transform-hover`, `--button-transition-duration`), and both are dropped
under `prefers-reduced-motion: reduce`.

## Above the page

The surface ramp and the lift both work *within* the page. One step goes above it: a modal,
which separates itself with `--color-scrim` rather than with elevation. The scrim is the only
axis-independent color role in the system — deep ink at 86%, identical in all six theme × mode
combinations — because a dim that shifted with the theme would read as a different depth each
time you opened it.

Modal is its only consumer, and it needs no z-index at all: `showModal()` puts the dialog in
the browser's top layer, which sits above every stacking context on the page. Note the
consequence for the panel's edge — on a deep scrim, dark mode's cream border and shadow read
clearly while light mode's ink ones do not, so in light the panel's white silhouette is the
edge. See **Components → Modal**.
