{
  "$comment": "Component tokens reference semantic tokens only — never primitives, never raw values.",
  "icon": {
    "size-sm": { "value": "{size.icon.sm.value}" },
    "size-md": { "value": "{size.icon.md.value}" },
    "size-lg": { "value": "{size.icon.lg.value}" },
    "$comment": "Icons inherit colour from their text context by default; these are the explicit escapes.",
    "color-default": { "value": "{color.text.primary.value}" },
    "color-muted": { "value": "{color.text.muted.value}" },
    "color-accent": { "value": "{color.text.accent.value}" }
  }
}
