{
  "$comment": "Component tokens reference semantic tokens only — never primitives, never raw values.",
  "button": {
    "radius": { "value": "{radius.control}" },
    "border-width": { "value": "{border.width.default}" },
    "font-family": { "value": "{font.family.label}" },
    "font-size": { "value": "{font.size.body-secondary}" },
    "font-weight": { "value": "{font.weight.control}" },
    "$comment": "Mono uppercase always carries the wide label tracking — without it the caps set too tight to read.",
    "tracking": { "value": "{font.tracking.label}" },
    "min-height": { "value": "{size.control}" },
    "padding-x": { "value": "{space.inset}" },
    "padding-y": { "value": "{space.inset-tight}" },
    "gap": { "value": "{space.inline}" },
    "shadow-hover": { "value": "{shadow.button-lift}" },
    "transform-hover": { "value": "{motion.button-lift}" },
    "hover-brightness": { "value": "{effect.button-hover-brightness}" },
    "$comment-pressed": "Pressed is the inverse of hover — the sticker settles back down. It resolves to the resting values in every theme, so a theme that lifts collapses the lift, and one that brightens (Adventure) drops back to no bump.",
    "shadow-pressed": { "value": "{shadow.flat}" },
    "transform-pressed": { "value": "{motion.transform.rest}" },
    "pressed-brightness": { "value": "{effect.brightness-rest}" },
    "transition-duration": { "value": "{motion.duration.interaction}" },
    "transition-easing": { "value": "{motion.easing.interaction}" },
    "$comment-spinner": "Loading spinner — a ring in the button's own text color (currentColor) that spins while an action is in flight. Sized to the inline icon so it sits where a leading icon would; ambient duration/easing because it is a continuous, non-reactive loop, the same motion category as the danger drift.",
    "spinner-size": { "value": "{size.icon.sm}" },
    "spinner-width": { "value": "{border.width.default}" },
    "spinner-duration": { "value": "{motion.duration.ambient}" },
    "spinner-easing": { "value": "{motion.easing.ambient}" },
    "primary": {
      "bg": { "value": "{color.action.primary.bg}" },
      "fg": { "value": "{color.action.primary.fg}" },
      "$comment": "Ink border is a global sticker rule — primary and secondary differ by fill, not by outline. The hard shadow extrudes from this edge.",
      "border": { "value": "{color.border.default}" }
    },
    "secondary": {
      "bg": { "value": "{color.action.secondary.bg}" },
      "fg": { "value": "{color.action.secondary.fg}" },
      "border": { "value": "{color.border.default}" }
    },
    "$comment-danger": "Danger is primary's shape in a different register: same footprint, same ink border, but the fill is the DANGER status rather than the brand — so it stays red in a theme whose brand isn't. The hazard stripes are a background-image over that fill, in a deepened step of the same red; the stripe geometry is a token so the texture scales with the system rather than being a magic number in the CSS.",
    "danger": {
      "bg": { "value": "{color.status.danger.bg}" },
      "fg": { "value": "{color.status.danger.fg}" },
      "border": { "value": "{color.border.default}" },
      "stripe": { "value": "{color.status.danger.deep}" },
      "stripe-width": { "value": "{space.inline}", "comment": "8px band, so the diagonal repeats every 16px" },
      "stripe-duration": { "value": "{motion.duration.ambient}", "comment": "one full pass of the pattern" },
      "stripe-easing": { "value": "{motion.easing.ambient}" }
    },
    "ghost": {
      "bg": { "value": "{color.action.ghost.bg}" },
      "fg": { "value": "{color.action.ghost.fg}" },
      "fg-hover": { "value": "{color.action.ghost.fg-hover}" },
      "border": { "value": "{color.border.none}" }
    }
  }
}
