{
  "$comment": "MODE = dark. Neutrals only. This is Confetti's dark look and the fallback for any theme that doesn't bring its own — a near-neutral GRAY ramp on a near-black content ground (#101012), with true black below it as the recessed mat, cool off-white text, and the brand red left as the one warm thing on the page. Light mode is warm cream paper; the two modes are deliberately different temperatures rather than one being a tinted copy of the other. How light `card` is drives almost everything else here: it is the lightest surface, so it sets the contrast floor, and the palest brand red is what caps it. Subtle accent tints deepen (X.700) so the light foreground reads on them; the hover shadow repoints to chalk, since a dark shadow on a dark surface reads as nothing. Emitted under [data-mode='dark'].",
  "color": {
    "surface": {
      "page": { "value": "{color.graphite.800}" },
      "raised": { "value": "{color.graphite.700}" },
      "panel": { "value": "{color.graphite.500}" },
      "card": { "value": "{color.graphite.400}" },
      "backdrop": { "value": "{color.graphite.900}", "comment": "recessed mat — the deepest graphite, below page. True black now that page sits at #101012, which gives this role real depth to work with again." }
    },
    "text": {
      "primary": { "value": "{color.chalk.100}" },
      "muted": { "value": "{color.chalk.400}" },
      "inverse": { "value": "{color.graphite.800}" }
    },
    "border": {
      "default": { "value": "{color.chalk.100}" },
      "subtle": { "value": "{color.graphite.a16}" },
      "none": { "value": "{color.base.transparent}" }
    },
    "action": {
      "secondary": {
        "bg": { "value": "{color.graphite.500}" },
        "fg": { "value": "{color.chalk.100}" }
      },
      "ghost": { "bg": { "value": "{color.base.transparent}" }, "fg": { "value": "{color.chalk.100}" } }
    },
    "$comment-status": "STATUS roles — see the note in modes/light.json. Only `text` moves between modes: the rich green FILL carries its white label at 5.34:1 on any page, but the same green AS TEXT drops to 2.43:1 on the card surface, so it lifts to green.400 here.",
    "status": {
      "success": {
        "bg": { "value": "{color.green.600}" },
        "fg": { "value": "{color.base.white}" },
        "text": { "value": "{color.green.400}" },
        "subtle": { "value": "{color.green.800}", "comment": "the tint end goes deep in dark, like the accent tints" },
        "on-subtle": { "value": "{color.chalk.100}" }
      },
      "danger": {
        "$comment": "See the note in modes/light.json — danger is caution tape as a FILL and red as TEXT. Dark takes the deeper yellow step: the light one glares against a near-black page.",
        "bg": { "value": "{color.yellow.600}", "comment": "hazard yellow, deepened for dark — carries a black label at 9.93:1" },
        "fg": { "value": "{color.base.black}", "comment": "black on the tape, the same pair as light" },
        "text": { "value": "{color.red.200}", "comment": "danger AS TEXT — the palest red, and the same lift brand-as-text takes. It has to be the palest one: red.300 fails on Confetti's `card` (3.50:1) and on Adventure's #35492f and Neon's #2e1650 too, so no theme on this mode can afford the more saturated step." },
        "deep": { "value": "{color.base.black}", "comment": "the hazard stripe on a danger control — full black on yellow, so the diagonal reads as tape rather than as texture" }
      }
    },
    "accent": {
      "purple": { "subtle": { "value": "{color.purple.700}" } },
      "teal": { "subtle": { "value": "{color.teal.700}" } },
      "orange": { "subtle": { "value": "{color.orange.700}" } },
      "pink": { "subtle": { "value": "{color.pink.700}" } },
      "on-bold": { "value": "{color.graphite.900}", "comment": "bold hues stay light in dark mode, so the deepest graphite reads on them" },
      "on-subtle": { "value": "{color.chalk.100}", "comment": "subtle tints are deep in dark mode — needs the light foreground" }
    }
  },
  "shadow": {
    "lift": { "value": "3px 3px 0px 0px {color.chalk.100}", "comment": "card hover shadow" },
    "button-lift": { "value": "3px 3px 0px 0px {color.chalk.100}", "comment": "button hover shadow — separate from card so a theme can drop or swap it" }
  },
  "focus": {
    "ring": {
      "width": { "value": "{border.width.sticker}", "comment": "2px" },
      "color": { "value": "{color.chalk.100}" },
      "offset": { "value": "{border.width.sticker}", "comment": "2px" }
    }
  }
}
